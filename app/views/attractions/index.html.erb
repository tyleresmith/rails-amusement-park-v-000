<% if @user.admin %>
  <%= link_to "New Attraction", new_attraction_path %>
<% end %>


<ul>
  <% @attractions.each do |attraction| %>
    <li><%= link_to "Show #{attraction.name}", attraction  if @user.admin%><%= link_to "Go on #{attraction.name}", attraction  if !@user.admin%></li>
  <% end %>
</ul>
